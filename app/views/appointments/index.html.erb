<body class="antialiased">
   <div class="container mx-auto px-4 sm:px-8">
      <%= render HeaderComponent.new(title: "Appointments")%>
      <div class="-mx-4 sm:-mx-8 px-4 sm:px-8 py-4 overflow-x-auto">
         <div class="inline-block min-w-full shadow rounded-lg overflow-hidden">

            <table class="min-w-full leading-normal">
               <thead>
                  <%= render 'appointments/table_column_headers' %>
               </thead>
               <tbody>
                  <%= render AppointmentComponent.with_collection(@appointments) %>
               </tbody>
            </table>
         </div>
                        <div class="px-5 py-5 bg-white flex flex-col xs:flex-row items-center xs:justify-between">
               <%== pagy_nav(@pagy)%>
            </div>
      </div>
   </div>
   </div>
   </div>
   </div>
</body>