<div class="flex flex-wrap -mx-3 mb-6">
  <div class="w-full px-3 mb-6 md:mb-0">
  <%= render ErrorComponent.new(resource: @appointment)%>
    <%= render HeaderComponent.new(title: "Update appointment for #{@appointment.patient.name}") %>
    <%= render "form", model: @appointment%>
  </div>
</div>
